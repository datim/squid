language: java
jdk:
  - oraclejdk8

# compile javascript
before_script:
 - npm install -g npm@2
 - webpack

# package UI and run
script:
 - ./gradlew test
 - ./gradlew build

# upload new build
after_success:
